pipeline {
    agent any

    stages {
        stage('Build') {
            agent {
                docker {
                    image 'my_playwright2'
                    reuseNode true
                }
            }
            steps {
                sh '''npm --version
                    node --version
                    npm ci
                    npm run build
                '''
            }
        }
    }
}
